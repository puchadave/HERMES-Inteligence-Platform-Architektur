[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "odysseus-edge-cli"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["httpx==0.28.1"]

[project.scripts]
odysseus-edge = "edge:main"

[tool.setuptools]
py-modules = ["edge"]
